[project]
name = "pingy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "icmplib>=3.0.4",
    "plotext>=5.3.2",
]

[project.scripts]
pingy = "main:run_cli"
